apply plugin: 'com.android.application'
apply plugin: 'newlens'

/*newlensExt {
    //newlensEnabled=false
    soDebugOpen = true
    symbolUploadEnabled = true
}*/
//apply plugin: 'com.bugtags.library.plugin'
/*bugtags {
    //自动上传符号表功能配置，如果需要根据 build varint 配置，请参考帮助中心->符号表->Android 符号表->配置自动上传符号表
    appKey "89261ba9df8c988a496b9472ed56ef07"  //这里是你的 appKey
    appSecret "661cbbf6c136c01f1637ac7483450ee2"    //这里是你的 appSecret，管理员在设置页可以查看
    mappingUploadEnabled true
}*/
project.ext.react = [
        entryFile: "index.js",
        enableHermes: false,  // clean and rebuild if changing
]
def jscFlavor = 'org.webkit:android-jsc:+'
def enableHermes = project.ext.react.get("enableHermes", false);

android {
    compileSdkVersion 28
    //避免stage编译报错"AAPT err(Facade for 2143981870): libpng error: Not a PNG file"
    aaptOptions.cruncherEnabled = false
    aaptOptions.useNewCruncher = false
    signingConfigs {
        yttConfig {
            storeFile file("yangtt.keystore")
            storePassword "123456"
            keyAlias "yangtt"
            keyPassword "123456"
        }
    }
    defaultConfig {
        applicationId "yangtt.personal.testjni_saas"
        minSdkVersion 23
        targetSdkVersion 28
        versionCode 1
        versionName "1.0.3"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        externalNativeBuild {
            cmake {
                cppFlags "-frtti -fexceptions"
            }
        }
        sourceSets {
            main.jniLibs.srcDirs = ['libs']
        }
        ndk {
            moduleName "Java2C" //so文件名
            abiFilters "armeabi-v7a", "x86", "x86_64", "arm64-v8a"//CPU类型
        }
    }
    buildTypes {
        debug {
            buildConfigField("boolean", "LOG", "false")
        }
        release {
            minifyEnabled true
            signingConfig signingConfigs.yttConfig
            buildConfigField("boolean", "LOG", "false")
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
        stage {
            buildConfigField("boolean", "LOG", "true")
        }
    }
    externalNativeBuild {
        cmake {
            path "CMakeLists.txt"
        }
    }
    compileOptions {
        sourceCompatibility = 1.8
        targetCompatibility = 1.8
    }
    buildToolsVersion = '28.0.3'
}
configurations.all {
    resolutionStrategy.cacheChangingModulesFor 0, 'seconds'
}
dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    api 'com.facebook.react:react-native:0.61.5'
    //compile(name: 'nbsnativecrashlib-release', ext: 'aar')
    compile(name: 'crashreport-3.0.0', ext: 'aar')

    //compile project(':react-native-tingyunapp')
    //implementation files('../node_modules/react-native-tingyunapp/android/libs/tingyun-ea-agent-android.jar')
    //implementation 'com.android.support:support-annotations:27.1.1'
    implementation 'androidx.annotation:annotation:1.1.0'
    //implementation 'com.android.support:appcompat-v7:26.1.0'
    implementation 'androidx.appcompat:appcompat:1.1.0'
    //implementation 'com.android.support.constraint:constraint-layout:1.1.3'
    implementation 'androidx.constraintlayout:constraintlayout:1.1.3'
    //implementation 'com.android.support:support-v4:26.1.0'
    implementation 'androidx.legacy:legacy-support-v4:1.0.0'
    //implementation 'com.android.support:recyclerview-v7:26.1.0'
    implementation 'androidx.recyclerview:recyclerview:1.1.0'
    testImplementation 'junit:junit:4.12'
    //androidTestImplementation 'com.android.support.test:runner:1.0.2'
    //androidTestImplementation 'androidx.test:runner:+'
    //androidTestImplementation 'com.android.support.test.espresso:espresso-core:3.0.2'
    //androidTestImplementation 'androidx.test.espresso:espresso-core:+'
    //implementation 'com.squareup.okhttp3:okhttp:3.10.0'
    implementation 'com.squareup.okio:okio:1.13.0'
    implementation 'com.facebook.react:react-native:+'
    if (enableHermes) {
        def hermesPath = "../node_modules/hermes-engine/android/";


        debugImplementation files(hermesPath + "hermes-debug.aar")
        releaseImplementation files(hermesPath + "hermes-release.aar")
    } else {
        implementation jscFlavor
    }
    //选择器第三方库
    //compile 'com.github.addappcn:android-pickers:1.0.1'
    //tingyun
    //implementation 'com.networkbench.newlens.agent.android:nbs.newlens.agent:2.14.4'
    implementation 'com.networkbench.newlens.agent.android:nbs.newlens.nativecrash:1.2.1'
    //bugly
    //compile 'com.tencent.bugly:crashreport:latest.release'
    //compile 'com.tencent.bugly:crashreport:latest.release'
    //compile 'com.tencent.bugly:nativecrashreport:latest.release'
    //bugtags
    //compile 'com.bugtags.library:bugtags-lib:3.0.0'
    //implementation files('libs/newOkhttp.jar')
    //implementation files('libs/okhttp-3.12.0.jar')
    //implementation files('libs/nbs.newlens.agent.jar')


}
